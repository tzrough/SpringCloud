server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:tzrough/SpringCloudConfig.git
          search-paths: config
          username:
          password:
      label: master
